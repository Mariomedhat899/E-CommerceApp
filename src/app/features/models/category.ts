export interface Category {
    results: number
    data:[]
  }
  

  
  export interface data {
    _id: string
    name: string
    slug: string
    image: string
    createdAt: string
    updatedAt: string
  }
  